\documentclass{exam}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\renewcommand{\thepartno}{\roman{partno}}

\begin{document}
\centering
\makebox[\textwidth]{\textsc{Math 5010 - Spring 2023 (Prof. Kinser) - Quiz 7}}
\vspace{1em}

\begin{questions}
    \question[3]
        Let $F$ be a field, $K$ an extension of $F$, and $p(x)$ a polynomial over $F$.
        \begin{parts}
            \part
                State what it means for $p(x)$ to be \textit{separable} over $F$.
                \vspace*{6cm}
            \part
                State what it means for $K$ to be \textit{separable} (or \textit{separably algebraic}) over $F$.
                \vspace*{6cm}
        \end{parts}
    \question[2]
        For $n>1$, state an equivalent expression for $x^n-1\in\mathbb{Z}[x]$ using either the $n$-th root of unity $\omega = e^{2\pi i/n}$, 
        or using the cyclotomic polynomials $\Phi_k(x)$ (you do not need to wirte any precise cyclotomic polynomial and you are not limited to one).
\end{questions}

\end{document}